-- ============================================================
-- FRESH INSTALL - deletes old database and rebuilds everything
-- WARNING: This removes ALL existing EcoSprout data
-- ============================================================

DROP DATABASE IF EXISTS ecosprout;

-- Now open schema.sql and sample_data.sql and run them in order
-- Or paste both files one after another in phpMyAdmin SQL tab
